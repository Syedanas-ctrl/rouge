export enum BuilderTabs {
    API = "API",
    JS = "JS",
    UI = "UI",
}

export enum BuilderComponents {
}

export enum ComponentTypes {
    Featured = "Featured",
    Button = "Button",
    Input = "Input",
    Select = "Select",
    Display = "Display",
    Layout = "Layout",
    Media = "Media",
    Toggle = "Toggle",
    Slider = "Slider",
    Content = "Content",
    External = "External",
}